{
  "name": "magic-frame-mvg-widget",
  "version": "0.1.0",
  "private": true,
  "description": "MVG departures widget for Magic Frame",
  "scripts": {
    "build": "node ../magic-frame/scripts/build-module.mjs src/widget.tsx dist",
    "postbuild": "node scripts/upload-module.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "pg": "^8.13.0",
    "typescript": "^5.6.0"
  }
}
